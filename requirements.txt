# LIFE dependencies
# Install: pip install -r requirements.txt

# Semantic search (meaning-based memory retrieval)
sentence-transformers
fastapi
uvicorn

# Dashboard visualization (drives:start visual, state render)
matplotlib
numpy

# Vision (webcam, screenshots, image viewing)
opencv-python
pillow

# Voice (speak + listen, requires OpenAI API key)
openai
sounddevice
soundfile
